export const metadata = {
  title: 'Agendamento Online',
  description: 'Agende seu horário no salão',
};

export default function AgendarLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
